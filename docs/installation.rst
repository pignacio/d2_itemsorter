============
Installation
============

At the command line::

    $ easy_install d2_itemsorter

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv d2_itemsorter
    $ pip install d2_itemsorter
